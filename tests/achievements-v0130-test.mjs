import assert from "node:assert/strict";import{readFileSync}from"node:fs";const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),roll=read("supabase/functions/roll/index.ts"),features=read("supabase/functions/features/index.ts"),ui=read("achievements/achievements.js"),sql=read("supabase/migrations/20260826000001_achievements_v0130_beta.sql");assert.ok(!/if \(!saved\.reward_granted\)/.test(roll));assert.match(features,/achievement-claim/);assert.match(features,/achievement-milestone-claim/);assert.match(ui,/data-claim/);assert.match(ui,/data-milestone/);assert.match(sql,/auth\.uid\(\)is distinct from p_player_id/);assert.match(sql,/achievement_points_awarded/);assert.match(sql,/milestone_already_claimed/);console.log("Achievement v0.13.0 claim-path tests passed.");
