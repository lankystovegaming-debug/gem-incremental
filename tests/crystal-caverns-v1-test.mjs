import assert from "node:assert/strict";import fs from "node:fs";import{crystalBaseProgress,crystalRollGains,crystalArtifactMultiplier,crystalSeverityProbabilities}from"../src/logic/crystalCaverns.js";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),baseSql=read("supabase/migrations/20260829042000_crystal_caverns_v1.sql"),rework=read("supabase/migrations/20260831150000_crystal_extraction_progress_rework.sql"),sql=baseSql+rework,client=read("src/backend/cloudExpeditions.js"),page=read("crystal-caverns/crystal-caverns.js"),dialog=read("src/ui/dialog.js"),roll=read("supabase/functions/roll/index.ts");
for(const table of["crystal_cavern_runs","crystal_cavern_artifacts"])assert.match(sql,new RegExp(`create table public\\.${table}`));
for(const rpc of["start_crystal_caverns","fund_crystal_depth","record_crystal_cavern_roll","resolve_crystal_decision","continue_crystal_overdepth","extract_crystal_caverns","settle_crystal_caverns","get_crystal_caverns_dashboard"])assert.match(sql,new RegExp(`function public\\.${rpc}`));
for(const n of[200000,300000,450000,650000,900000,1300000,2000000,3000000,4500000,7000000])assert.match(sql,new RegExp(String(n)));
for(const n of[12000000,18000000,26000000,38000000,54000000,76000000,108000000,152000000,214000000,300000000])assert.match(sql,new RegExp(String(n)));
assert.match(sql,/3-2\*exp\(-greatest\(0,p_i\)::numeric\/125\)/);assert.match(sql,/\.65\*exp\(-power\(greatest\(0,p_i\)::numeric\/145,1\.406\)/);assert.match(sql,/\.08\+\.92\*\(1-exp\(-power\(greatest\(0,p_i\)::numeric\/320,1\.767\)\)\)/);
for(const artifact of["crystal-splinter","heart-of-the-cavern","impossible-crystal","shattered-heart"])assert.match(sql,new RegExp(artifact));
assert.match(sql,/array\['pocket','resonance','deposit'\]/);assert.match(sql,/random\(\)<\.35/);assert.match(sql,/r\.instability:=r\.instability\+30/);assert.match(sql,/if r\.status<>'forced_extraction'then r\.unsecured_cargo/);assert.match(sql,/on conflict\(player_id,artifact_key\)do nothing/);
for(const rpc of["get_crystal_caverns_dashboard","start_crystal_caverns","fund_crystal_depth","resolve_crystal_decision","continue_crystal_overdepth","extract_crystal_caverns","settle_crystal_caverns"])assert.match(client,new RegExp(rpc));
assert.match(page,/Instability is unchanged/);assert.match(page,/FRACTURE THE HEART/);assert.match(roll,/crystal_player_effects/);assert.match(roll,/rolledWeightMultiplier >= 2 \? crystalHeavyGemValueMultiplier/);
assert.equal(crystalBaseProgress({rarity:49}),1);assert.equal(crystalBaseProgress({rarity:50}),2);assert.equal(crystalBaseProgress({rarity:1000}),5);assert.equal(crystalBaseProgress({rarity:10000,mutationIds:["x"],finalWeight:200,baseWeight:100}),18);
assert.deepEqual(crystalRollGains({rarity:50},"forceful"),{baseProgress:2,progress:2.5,instability:.08});
assert.deepEqual(crystalRollGains({rarity:1000},"fracturing"),{baseProgress:5,progress:8,instability:.5});
assert.equal(crystalRollGains({rarity:50},"forceful").instability+crystalRollGains({rarity:50},"fracturing").instability,.28,"fractional Instability must survive intensity switching");
assert.ok(Math.abs(crystalArtifactMultiplier(125)-(3-2/Math.E))<1e-12);const severity=crystalSeverityProbabilities(100);assert.ok(Math.abs(severity.minor+severity.major+severity.critical-1)<1e-12);
for(const text of["Extraction Progress","Expedition Cargo","Artifact chance","Recent Expedition Log","AT RISK","Duplicate","nextOverdepthDanger","defaultAction:\"cancel\"","preventEnter:true"])assert.match(page,new RegExp(text));
for(const cost of[250000,500000,1000000,1750000,3500000,5500000])assert.match(page,new RegExp(String(cost)));
assert.match(rework,/alter column progress type numeric/);assert.match(rework,/alter column instability type numeric/);assert.match(rework,/rarity'\)::numeric,0\)>=50/);assert.match(rework,/rarity'\)::numeric,0\)>=1000/);assert.match(rework,/rarity'\)::numeric,0\)>=10000/);assert.match(rework,/base_progress\*case r\.intensity/);assert.match(rework,/instability_gain:=base_progress\*case r\.intensity/);assert.match(rework,/nullif\(p_payload->>'gemName',''\)is null then return/);assert.match(rework,/final displayed weight \/ base weight/);
assert.match(client,/set_crystal_intensity/);assert.match(dialog,/preventEnter/);assert.match(dialog,/defaultAction/);
assert.equal((roll.match(/luck \*= researchNumber\("luck_multiplier"\)/g)||[]).length,1,"research Luck must be applied exactly once");
assert.match(roll,/luck \+= crystalLuckBonus;[\s\S]*luck \+= expeditionArtifactLuckBonus;/);
assert.match(roll,/mutationChanceMultiplier \*= crystalMutationMultiplier;[\s\S]*mutationChanceMultiplier \*= expeditionArtifactMutationMultiplier;/);
assert.match(roll,/crystalGemValueMultiplier \*[\s\S]*expeditionArtifactGemValueMultiplier \*[\s\S]*crystalHeavyGemValueMultiplier[\s\S]*bedrock-crown/);
console.log("Crystal Caverns V1 tests passed.");
